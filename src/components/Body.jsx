import MainContainer from "./MainContainer";
import SideBar from "./SideBar";

const Body = () => {
  return (
    <div>
      {" "}
      this is body
      <SideBar />
      <MainContainer />
    </div>
  );
};

export default Body;
